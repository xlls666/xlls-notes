FROM python:3.10-slim

WORKDIR /app

# 复制依赖文件并安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目代码
COPY . .

# 暴露端口
EXPOSE 20002

# 启动命令
CMD ["uvicorn", "rag_api:app", "--host", "0.0.0.0", "--port", "20002"]